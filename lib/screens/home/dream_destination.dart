import 'package:flutter/material.dart';

class DreamDestination extends StatelessWidget {
  const DreamDestination({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
      title: const Text('Dream Destination'),
      ),
    );
  }
}